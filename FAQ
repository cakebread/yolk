

Q: Why don't you list elementtree as a dependency with setuptools? 
A: Because most distributions already have elementtree. If I set it as a dependency and elementtree
wasn't installed by setuptools yolk will fail to import it.


